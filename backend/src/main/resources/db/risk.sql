CREATE MEMORY TABLE RISK_DRIVER(
    TEST_ID VARCHAR(100) NOT NULL PRIMARY KEY,
    DRIVER_ID VARCHAR(100),
    TEST_DATE VARCHAR(20),
    AGE VARCHAR(10),
    DOMAIN VARCHAR(10),
    SCORE DOUBLE,
    RISK_GROUP VARCHAR(20),
    RELATIVE_GROUP VARCHAR(20),
    FEATURE_JSON LONGVARCHAR
);
